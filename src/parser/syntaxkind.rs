#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Debug)]
#[repr(u16)]
pub enum SyntaxKind {
    TOMBSTONE,
    EOF,
    RULE,
    STRINGS,
    CONDITION,
    AND,
    OR,
    NOT,
    IDENTIFIER,
    VARIABLE,
    STRING,
    ASSIGN,
    COLON,
    LBRACE,
    RBRACE,
    LPAREN,
    RPAREN,
    COMMA,
    NUMBER,
    TRUE,
    FALSE,
    WHITESPACE,
    COMMENT,
    MULTILINECOMMENT,
    ERROR,
}
